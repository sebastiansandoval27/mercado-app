import styled from "styled-components";

export const GridMainStyled = styled.div`
  width: 100%;
  height: auto;
  display: grid;
  grid-template-columns: repeat(auto-fit, 250px);
  grid-template-rows: auto;
  justify-content: center;
  align-items: center;
  gap: 10px;
`;
